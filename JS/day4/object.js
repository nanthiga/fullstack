
let obj ={
    tamil:90,
    english:80,
    maths:10,
}
let values = Object.values;
console.log(obj); 
console.log(Math.round(3.45)); // it is used to round off the value eg:3.88880 means 4;3.49 means 3
console.log(Math.random()*100); // it is used to display random number 0 to 100 
console.log(Math.round(Math.random()*100)); // it is used display random number and it also round off it
console.log(Math.floor(1.01)) // to the ground
console.log(Math.ceil(1.01))  // to the ceiling
let arr = [1,2,3,4,5,6,9]
console.log(Math.min(...arr)) // to separate the using separat operator 
console.log(Math.max(...arr))
console.log(Math.abs(-3))