// array method
let arr = [1,2,3,4,5];
// arr.push(6);                                  //it is used to add an value in end
// arr.unshift(0);                               //it is used to add an value in start
// arr.pop();                                    //it is used to remove an value in end
// arr.shift();                                  //it is used to remove an value in start
 arr = arr.concat([7,8,9,10,21,32])              //it is used to concat two arr
console.log(arr[5])                              //it is separate the arr
console.log(arr.sort())                          //it is used to sort the arr eg[1,2,3,11,22,33] -> the op is [1,11,2,22,3,33u]
console.log(arr.slice(1,3));                     //it is used to slice the last element eg[1,2,3,11,22,33]->the op is [2,3]
